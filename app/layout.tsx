import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mobile Welding Services Edmonton | Darsco Welding",
  description: "Professional mobile welding and fabrication services for Edmonton and area. 24/7 emergency service, certified welders, quality guaranteed. Call (780) 405-3244.",
  keywords: "mobile welding Edmonton, welding services Alberta, fabrication, structural welding, MIG TIG welding",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
